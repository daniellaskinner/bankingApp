const displayAllAccounts = require("../displayAllAccounts");

describe('displayAccounts', () => {
    test('name', () => {
        expect(sum(params)).toBe(value);
    });

    test('name', () => {
        expect(sum(params)).not.toBe(value);
    })
});
